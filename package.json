{
  "name": "begin-app",
  "version": "0.0.1",
  "description": "Begin basic CRUD app",
  "scripts": {
    "start": "cross-env NODE_ENV=testing npx sandbox"
  },
  "devDependencies": {
    "@architect/sandbox": "^1.7.0",
    "cross-env": "^7.0.0"
  }
}
